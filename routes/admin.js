const express=require('express'); const jwt=require('jsonwebtoken'); const User=require('../models/User'); const {requireAdmin}=require('../middleware/adminAuth'); const router=express.Router();
router.post('/login',(req,res)=>{const email=String(req.body.email||'').trim().toLowerCase();const password=String(req.body.password||'');if(email!==String(process.env.ADMIN_EMAIL||'').trim().toLowerCase()||password!==String(process.env.ADMIN_PASSWORD||''))return res.status(401).json({error:'Invalid admin credentials'});const token=jwt.sign({role:'admin',email},process.env.JWT_SECRET,{expiresIn:'8h'});res.json({message:'Admin login successful',token});});
router.get('/stats',requireAdmin,async(req,res,next)=>{try{res.json({totalUsers:await User.countDocuments()});}catch(e){next(e);}});
router.get('/users',requireAdmin,async(req,res,next)=>{try{const page=Math.max(Number(req.query.page)||1,1),limit=Math.min(Math.max(Number(req.query.limit)||20,1),100),search=String(req.query.search||'').trim();const filter=search?{$or:[{name:{$regex:search,$options:'i'}},{gmailId:{$regex:search,$options:'i'}}]}:{};const [users,total]=await Promise.all([User.find(filter).select('-__v').sort({createdAt:-1}).skip((page-1)*limit).limit(limit),User.countDocuments(filter)]);res.json({users,pagination:{page,limit,total,totalPages:Math.ceil(total/limit)}});}catch(e){next(e);}});
router.get('/users/:id',requireAdmin,async(req,res,next)=>{try{const user=await User.findById(req.params.id).select('-__v');if(!user)return res.status(404).json({error:'User not found'});res.json({user});}catch(e){if(e.name==='CastError')return res.status(400).json({error:'Invalid user ID'});next(e);}});
router.patch('/users/:id',requireAdmin,async(req,res,next)=>{try{const update={};if(req.body.name!==undefined){const name=String(req.body.name).trim();if(!name)return res.status(400).json({error:'name cannot be empty'});update.name=name;}if(req.body.gmailId!==undefined){const gmailId=String(req.body.gmailId).trim().toLowerCase();if(!/^[a-zA-Z0-9._%+-]+@gmail\.com$/.test(gmailId))return res.status(400).json({error:'gmailId must be a valid Gmail address'});update.gmailId=gmailId;}const user=await User.findByIdAndUpdate(req.params.id,update,{new:true,runValidators:true}).select('-__v');if(!user)return res.status(404).json({error:'User not found'});res.json({message:'User updated',user});}catch(e){if(e.code===11000)return res.status(409).json({error:'Gmail ID already exists'});if(e.name==='CastError')return res.status(400).json({error:'Invalid user ID'});next(e);}});
router.delete('/users/:id',requireAdmin,async(req,res,next)=>{try{const user=await User.findByIdAndDelete(req.params.id);if(!user)return res.status(404).json({error:'User not found'});res.json({message:'User deleted'});}catch(e){if(e.name==='CastError')return res.status(400).json({error:'Invalid user ID'});next(e);}});
module.exports=router;
